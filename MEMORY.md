# ROKT — Shared Memory

Running log of durable decisions and learnings, shared across Claude Cowork and Claude Code. Append new entries at the top with a date. Keep entries short — link out to docs/files for detail. This is separate from Cowork's internal Memory feature and Claude Code's local auto-memory; both tools should write here instead so context stays unified.

---

- **2026-07-06** — **Cloudflare Pages IS set up and auto-deploying** (corrects an earlier in-conversation assumption that it wasn't). Confirmed via a screenshot of the Cloudflare dashboard:
  - Cloudflare account is under **Carol's email**: `Rootsofknowledgetutor@gmail.com`.
  - Pages project **`roots-of-knowledge`** is connected to the `sivhold/roots-of-knowledge` GitHub repo and **auto-deploys on every push to `main`** (a push this session triggered a build within ~5 min).
  - **Live preview URL: https://roots-of-knowledge.pages.dev/** — this is Cloudflare's default `.pages.dev` domain, NOT the custom domain. `roots-of-knowledge.com` still serves the WordPress.com site until the domain is deliberately cut over.
  - Expected build config (standard for this static-export app; not re-verified in the dashboard this session): root directory `code`, build command `npm run build`, output directory `out`.
  - The older **`rokt-hello-world`** Pages project also still exists (the throwaway test repo, ~17 days idle) — consistent with the 2026-06-21 git note; leave as-is.
  - ⚠️ The 2026-06-21 "Phase B design session" entry below describes the *original* Header/Footer/layout (green active pill, pop-out hover, vertical-centering wrapper). Those were **replaced 2026-07-06** by the Claude-Design redesign handoff (`brain/redesign-handoff/`) — new Header, Footer, Home, and About pages. See git history (`91acb5f`, `099f6d2`, `8ec871b`) for the current implementation.

---

- **2026-07-06** — **Redesign session: Header, Footer, Home, and About rebuilt from the Claude-Design handoff.** Geoff redesigned the site in the Claude Design app; the handoff package now lives in `brain/redesign-handoff/` (README spec + `.dc.html` visual references + logo asset). It is a **design spec, not runnable code** — `.dc.html` files use a prototyping-tool templating syntax. Implemented it as real Next.js components, matching the codebase's inline-style convention and existing `rokt-*` CSS variables. What changed:
  - **Fonts:** added **Lora** (headings + header wordmark) and **Source Sans 3** (body/nav/buttons) via `next/font/google`, exposed as CSS vars `--font-lora` / `--font-source-sans` in `layout.tsx` (replaced the leftover create-next-app Geist fonts). Global `body` font is still Arial — flip to Source Sans 3 in a later global pass.
  - **Header:** 88px sticky bar; logo + two-line "Roots of Knowledge / TUTORING" wordmark; nav hover = green tint `rgba(61,122,69,0.3)`; **active state = underline** (replaced the old green pill); **Donate = green pill CTA**; nav + Donate sized to **1.2rem** (Geoff asked for larger). Dropped the old pop-out/scale hover.
  - **Footer:** simplified centered layout (copyright · email · social), subtle `opacity:0.5` bullets, 1.2rem text. Social icons swapped to **official brand marks in brand colors** — Facebook white "f" on `#1877F2`, **X white glyph on black** (note: X is black now, superseding the todo's old "Twitter blue #1DA1F2"). Still `href="#"` placeholders until Carol provides real URLs.
  - **Home (`app/page.tsx`):** rebuilt as 4 stacked sections — Hero (gradient, logo, Lora subheadline, Get Help + Support Our Work CTAs), Mission band, How We Work (In Person / Virtual cards with striped photo *placeholders*), Be Part of It (3 numbered cards → Enroll/Donate/Volunteer). Shipped the design's "cards" variant.
  - **About (`app/about/page.tsx`, new `/about` route):** Intro, Founder Story (Carol's real photo + "Our Story" ×4 paras), Mission & Vision cards, Core Values 2×2, CTA band. Added `metadata` title/description.
  - **Carol's photo:** she provided one → `public/carol-bluee.jpg` (290×297, ~42KB, near-square). **Kept as JPEG** — static export (`images.unoptimized`) serves it untouched, so conversion offers no real gain; a higher-res portrait would drop in cleanly later. Fold fix (industry-standard): capped display to 320×400, `object-position: center 30%` (face focal point), and trimmed intro/founder vertical padding so the full portrait clears the fold on landing instead of showing just her forehead.
  - **Refactor:** extracted the hover-button helper into `components/HoverLink.tsx` (shared by Home + About); **Home became a server component** as a result. Fixed a React 19 warning by switching the `textDecoration` shorthand → `textDecorationLine` longhand in Header/Footer/HoverLink.
  - **Tooling:** added `.claude/launch.json` (dev preview: `npm --prefix code run dev`, port 3000); gitignored machine-local `.claude/settings.local.json`.
  - **Commits (all on `main`, pushed):** `91acb5f` header/footer · `099f6d2` home · `8ec871b` about. Auto-deployed live to https://roots-of-knowledge.pages.dev/ (verified `/about` serving the redesign).
  - **Deliberately left uncommitted:** pre-existing edits to `CLAUDE.md`, `brain/tech-stack-rationale.md`, and the three `todo/*.md` files (predate this session; kept out of the redesign commits).
  - **Next:** Donate page (the most interactive — amount picker, payment cards, coming-soon modal, PayIcon badges). Sidecar `.md` docs for the new/changed code files are still outstanding (tracked in `todo-nextjs-design.md`).

---

- **2026-06-21** — **Phase B design session: color system + global layout complete.** Full session summary:
  - Picked `rokt-accent: #3d7a45` — sourced from Carol's existing flyer (section headers, icons, callout boxes). Confirmed same green also appears in logo underline/wordmark. Documented in `brain/design-tokens.md`.
  - Added two new named font tokens — `rokt-header-font` and `rokt-footer-font` (both `#fdf6ec`) — named separately from `rokt-light` so header/footer text color can be changed independently later.
  - **CSS variable gotcha:** Tailwind v4 `@theme` tokens are NOT reliably available via `var()` in inline styles. Fix: declare all tokens in both `@theme` AND `:root` in `globals.css`. See `brain/design-tokens.md` for full note.
  - Built `code/components/Header.tsx` (client component — needs `usePathname`): sticky, `rokt-dark` bg, site title links to `/`, nav links with active green pill (`rokt-accent`) and pop-out hover animation (translateY -3px, scale 1.06, drop shadow).
  - Built `code/components/Footer.tsx`: `rokt-dark` bg, centered single line — copyright, email link (green underline), Facebook + Twitter placeholder icons.
  - Updated `code/app/layout.tsx`: imports Header + Footer, wraps `{children}` in a `margin: auto` flex div for automatic vertical centering — short pages center between header/footer, tall pages align top and scroll.
  - Dev server port note: if port 3000 is already in use (e.g. a leftover node process from a prior session), Next.js auto-increments to 3001. Kill the stale process first (`netstat -ano | findstr :3000` to find PID, then `Stop-Process -Id <PID> -Force`) so `npm run dev` always claims 3000.

---

- **2026-06-21** — **Git initialized for ROKT.** Repo created and pushed to GitHub. Key details:
  - GitHub account: `sivhold` (Geoff's personal account / Sivhold LLC)
  - Repo URL: `https://github.com/sivhold/roots-of-knowledge.git`
  - Branch: `main`
  - Auth method: Personal Access Token (classic), `repo` scope only, stored in Geoff's password manager. Token is account-level (works across all sivhold repos). To push, use: `git push https://sivhold:TOKEN@github.com/sivhold/roots-of-knowledge.git main` — or configure Windows Credential Manager to cache it.
  - `.gitignore` covers: `node_modules/`, `.next/`, `out/`, `.env*`, `.DS_Store`, `Thumbs.db`, `.vscode/`, Obsidian cache/workspace files.
  - Old test repo (`rokt-hello-world`) still exists on GitHub/Cloudflare Pages — deliberately left as-is, not connected to this project.
  - Next step: configure Windows Credential Manager so git remembers the token automatically (not yet done as of this entry).

---

- **2026-06-21** — **Phase B started early.** Geoff is switching to Claude Code now to begin actual Next.js implementation, ahead of the original ~10-month-out / WP.com-renewal-timed plan. Confirmed explicitly (not assumed) before updating `CLAUDE.md`'s "Two-phase plan & current status" section — Phase A (WordPress.com site) stays active in parallel, not paused. First concrete build step is `todo/todo-nextjs-migration.md` § 3 (scaffold via `npx create-next-app@latest` into `code/`).
- **2026-06-21** — Decided Phase B scaffolding tool: **`npx create-next-app@latest`** (official Next.js scaffolder), flagged for App Router + TypeScript + Tailwind, run into `code/` when Phase B starts. Compared against cloning a Vercel template-gallery repo and against Claude hand-building the skeleton — official npx tool wins on staying current with Next.js conventions and avoiding manual-config risk. Full rationale in `brain/tech-stack-rationale.md`.
- **2026-06-21** — Decided Phase B CSS tech: **Tailwind CSS** (Geoff already knows it from prior React work). Reopened and reconsidered against the newer 2023–2026 wave of zero-runtime CSS-in-JS tools (vanilla-extract, Panda CSS, Meta's StyleX) before confirming — rejected those because they add a build-time dependency/file convention beyond Tailwind, and Tailwind's "long class strings hurt maintainability" critique mainly applies at large/dynamic-component scale, not ROKT's small static site. Full comparison and rationale in `brain/tech-stack-rationale.md` (new file). Component library choice (shadcn/ui etc.) still open, deferred to Phase B scaffolding. CLAUDE.md's existing Phase B stack line already said Tailwind, so no change needed there — this entry documents that it was deliberately re-derived, not just inherited.
- **2026-06-21** — Processed a 2-file `inbox/` batch: `ROKT-Flyer.jpg` (Carol's marketing flyer) and `rokt-home-page-mockup.svg` (a wireframe mockup), both referenced-but-missing files flagged in the earlier 10-handoff import. Moved flyer → `docs/rokt-flyer.jpg` (raw source material); moved mockup → `brain/rokt-home-page-mockup.svg` (an authored design artifact, lives with `page-designs-and-copy.md`). The mockup's SVG source contained the full "Our Mission" and "How We Work" homepage copy that the earlier import couldn't find anywhere in the repo — recovered and added to `brain/page-designs-and-copy.md`. The flyer contributed a Services list and mission framing usable for the still-unbuilt Programs/Services page, also added there. `inbox/` cleared again.
- **2026-06-21** — Processed a 10-file `inbox/` batch of handoff `.md` files (one per past WordPress-era claude.ai Project conversation, per Geoff's "create a handoff-<conversation name>.md" prompt pattern). Per Geoff's instruction, kept only content usable for the Next.js port (layout, colors, copy structure) and discarded WordPress-only mechanics. Extracted durable design/content detail into new file `brain/page-designs-and-copy.md` (About/Donate/How-to-Give page structures, Home page hero + planned sections, two unconfirmed homepage layout concepts "Option A/B"). All 10 source files discarded from `inbox/` after extraction, consistent with the established inbox convention.
- **2026-06-21** — Confirmed the proposal-file discrepancy flagged in one imported handoff (an older unversioned `PROPOSAL.md` vs. the documented-current `brain/proposal-v2.md`) is already resolved — `proposal-v2.md` already contains the softened portfolio-use wording (§5), the Timeline section (§9), and the non-liability clause (§10 item 11) that the handoff described adding to the older file. No action needed; `proposal-v2.md` remains authoritative.
- **2026-06-21** — Discovered via an imported handoff (site review conversation) that the **live site (roots-of-knowledge.com) currently has no working navigation menu** — homepage drops straight into WordPress.com chrome with no clickable links to other pages — which contradicts an old (now-discarded) project-context file's claim that the header/nav was complete. Cause unconfirmed (reverted header, CSS/theme regression, or stale doc). Also found a live content bug: "Academic excellence and accountability" repeats three times on the homepage Core Values section. Both logged as open items — see chat for options presented to Geoff.
- **2026-06-20** — **Security:** `docs/website-design.docx` contains a live WordPress login (`https://wordpress.com`, password `[REDACTED]`) and a Pexels login, both in plain text. Surfaced while processing an `inbox/` import batch. Geoff should rotate the WordPress password and scrub/redact the credentials from the doc — never paste credentials into chat or commit them anywhere.
- **2026-06-20** — Processed an `inbox/` batch of 7 files (a fresh claude.ai Project export) per the `inbox/` convention. `PROPOSAL_v2.md` was an exact duplicate of `brain/proposal-v2.md` — discarded. The four `.docx`-named files (`ROKT_Contact`, `ROKT_RegForm`, `Website_design`, `rokt_donate`) were actually plain-text exports (not real Word files) duplicating content already in the real `.docx` files under `docs/` — discarded. `CLAUDE_v4.md`'s durable content (sponsorship tiers, Formspree/Stripe specifics) was already captured in `brain/proposal-v2.md`/`CLAUDE.md`, except the credential warning above — discarded after extracting that. `ROKT_PROJECT_CONTEXT_v3.md` had real new value — Additional CSS block, color palette, CoachAva theme quirks, design decisions, copy voice guide — none of which existed elsewhere in the current system; extracted into new file `brain/wordpress-site-reference.md`, then discarded. Fast-changing status (site map, open questions) was deliberately left out of the new file since `todo/todo-phase-a-website.md` already owns that. **Correction to the 2026-06-20 entry below claiming these old versioned files "no longer exist anywhere in the repo"** — that was true at the time but they resurfaced via a second inbox import; this entry supersedes it.
- **2026-06-20** — Added `inbox/` folder convention to `CLAUDE.md`: Geoff dumps new files there ad hoc (e.g. claude.ai Project exports); on request, go through each file one at a time and either move it to its proper folder or fold its content into the relevant existing doc, then clear `inbox/`.
- **2026-06-20** — Imported the standing instructions from the claude.ai ROKT Project. Two notable points: (1) it directed Todoist as the canonical tracker ("task tracking lives in Todoist, NOT in a PROJECTPLAN .md file") — this directly contradicted the same-day decision that markdown is now the ROKT standard; Geoff confirmed markdown stays the standard and this old instruction is superseded, but its operational Todoist details (parent/subtask only, no sections; `Housekeeping` and `Waiting on Carol` are persistent uncompletable containers; judgment call on which other tasks get that treatment) were kept in `CLAUDE.md` for whenever Todoist is touched directly. (2) Its "remind me to verify I'm using Sonnet" instruction was generalized at Geoff's request to "remind me to make sure I'm using an appropriate model" and added to `CLAUDE.md` as a session-start checklist item, applying across Claude Code, Cowork, and claude.ai.
- **2026-06-20** — Imported knowledge from a Claude Chat (claude.ai) conversation/memory dump covering Phase A build history that predates this `CLAUDE.md`/`MEMORY.md` system. Confirmed the old versioned project files it referenced (`CLAUDE_v4.md`, `ROKT_PROJECT_CONTEXT_v3.md`) no longer exist anywhere in the repo — they were fully superseded by the current unversioned `CLAUDE.md`/`MEMORY.md` plus `brain/proposal-v2.md` during the 2026-06-20 reorg. `proposal-v2.md` is confirmed as the latest/authoritative version (its `Stripe + Zelle/Cash App/check` donation framing matches and clarifies the `CLAUDE.md` donation-methods constraint — Stripe is additive, not a replacement). Older working style was "Todoist for task tracking, PROJECTPLAN markdown retired in favor of Todoist" — this has since reversed; markdown is now the standard (see 2026-06-20 entry below) and Todoist is the not-yet-migrated backlog.
- **2026-06-20** — Phase A WordPress build status as of the imported chat: **Done** — homepage hero section (gradient background, transparent PNG logo, subheadline, "Get Help"/"Support Our Work" CTAs, page title hidden via CSS); About Us page (card design, color `#A67535`, `32px` padding, `12px` border radius, cream text `#FDF6EC`, Mission/Vision side-by-side + full-width Core Values below, custom CSS class `mission-vision-row` for equal-height cards); Donate/Support Our Work page (Zelle, Cash App, mailed check; Stripe placeholder removed pending Carol); How to Give page at `/donation-instructions/` (Zelle/Cash App + Check by Mail cards, volunteer callout; all donation-amount buttons link here); Coming Soon page (linked from all unfinished nav items); global fixes via Additional CSS (page-title gradient `#6B4A2B`→`#C58F41` at Pages template level, sticky footer, body background, active menu underline). **Not yet done** — remaining homepage sections (Our Mission, How We Work, Be Part of It).
- **2026-06-20** — Open bug from imported chat: a golden band appears between the hero and footer on the homepage; CSS attempts so far haven't fully resolved it. Logged in `todo-phase-a-website.md`.
- **2026-06-20** — Open decision from imported chat: a widescreen homepage layout proposal (wireframe Options A and B) was generated for Carol's review; no direction confirmed yet. Logged in `todo-phase-a-website.md`.
- **2026-06-20** — Agreed page-build order for Phase A (from imported chat): Home (finish) → Programs → Enroll → Impact → Volunteer. About page also has two outstanding Todoist-logged issues: paragraph spacing and edge-to-edge text padding.
- **2026-06-20** — Todoist tool gotcha (from imported chat): `add-tasks`/`update-tasks` calls fail silently above ~13–17 tasks; batches of 8 are reliable. When reassigning `parentId`, must explicitly set `sectionId: ''` or the call fails. Relevant if/when the 57-task Todoist backlog gets migrated or manipulated via MCP.
- **2026-06-20** — Skill storage differs between tools: Claude Code reads global/personal skills from the local filesystem at `C:\Users\gvd10\.claude\skills\` (e.g. `align-before-action`), and project-only skills from `<repo>\.claude\skills\<skill-name>\SKILL.md` (confirmed via Claude Code docs — project skills apply to that repo only). Cowork has no local-filesystem or project-scoped skill mechanism — it only reads from an account-level, server-side skill library (managed via Settings > Capabilities in the Claude app), synced into every Cowork session regardless of folder. Net effect: a skill installed for Cowork is global across all of Geoff's projects, not just ROKT — there is currently no way to make a skill ROKT-only for Cowork while also having it usable by Claude Code. **When Geoff packages a new global skill (e.g. as a `.skill` file) for Claude Code's local folder, remind him it also needs a separate manual install into Cowork via Settings > Capabilities** — the two don't sync automatically.
- **2026-06-20** — Reorganized the ROKT root from a flat folder into: `code/` (future Phase B app code, empty for now), `docs/` (raw Word docs + original images/logos), `todo/` (renamed from `ToDo/`, unchanged purpose), `brain/` (cooperative decisions/approach/tech-stack rationale — `proposal-v2.md` and `copy-wordpress-content.md` moved here), `temp-code/` (disposable: `hello-world-test/`, `index.html`, mockup files — slated for eventual deletion). `CLAUDE.md`, `MEMORY.md`, and `.obsidian/` stay at root. Naming convention: lowercase kebab-case everywhere at this level, except `CLAUDE.md`/`MEMORY.md` (case required for tooling auto-discovery) and the contents of `code/` (follows JS/TS/Next.js conventions instead). Full rationale and the per-file mapping are in `CLAUDE.md` under "Folder & file structure." This reorg was done live as a demo of a new `align-before-action` skill (relentless clarifying-questions-before-acting) Geoff is building — see `[[feedback-align-before-action]]` if that memory exists.
- **2026-06-20** — Reorganized `ToDo/` into phased structure: `todo-master.md` (index) → `todo-phase-a-website.md` (active, Phase A WordPress.com build) and `todo-nextjs-migration.md` / `todo-copy-wordpress-content.md` (paused, Phase B). Obsidian Tasks plugin installed; use 📅 and ⏫🔼🔽⏬ syntax going forward.
- **2026-06-20** — Decided markdown files (not Todoist) are the standard for ROKT todo/planning content, because the work is repo-adjacent and benefits from linking tasks to docs/proposals inline, and because Claude Code gets native file access with zero extra setup (vs. Todoist needing a separate MCP connector configured inside Claude Code). Todoist's "Roots-of-Knowledge" project (57 tasks) still holds the actual current Phase A build backlog — not yet migrated, tracked as a low-priority item in `todo-phase-a-website.md`.
- **2026-06-20** — Created `CLAUDE.md` at the ROKT root as the single source of truth for project instructions, read automatically by Claude Code and (via a pointer instruction) by Claude Cowork. Cowork's own Instructions panel should stay reduced to a short bootstrap pointer rather than duplicating content, to avoid the two drifting out of sync. Confirmed Cowork's Instructions panel was fully replaced (not appended) with the pointer text — no duplication. Also observed Cowork appears to auto-surface `CLAUDE.md` content from the connected folder on its own, independent of the Instructions panel.
- **2026-06-20** — Claude Chat (claude.ai) integration deferred — user wants to handle it later but added a todo (`todo-claude-chat-integration.md`) to eventually bring in all ROKT context there too, including investigating folder access and automating the sync.
- **2026-06-20** — Geoff prefers replies structured as bullets/sub-bullets for ROKT work (easier to digest than prose). Added to `CLAUDE.md` under "Communication style." Applies to both Claude Code and Claude Cowork. Fall back to prose/other formats only when content doesn't fit bullets well.
